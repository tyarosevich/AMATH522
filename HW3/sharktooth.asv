function dy = sharktooth(t,y,p)
    if y(1) > 0
        z1=1;
    else
        z1=0;
    end
    if y(1) > 2
        z2 = 1;
    else
        z2=0;
    end
    dy = zeros(3,1);
    dy(1) = 1 - .3*y(1);
    dy(2) = ( (p(1)*( (y(2)+y(1))^p(4)))) / ((1 + y(1) + y(2))^p(4)) - y(2); 
    dy(3) = ( (z2 + y(3))) / (1+ y(3)) - y(;
    
    % This gave a tooth type re